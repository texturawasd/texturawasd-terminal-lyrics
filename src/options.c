#include <string.h>

#error UNIMPLEMENTED

void do_options(int argc, char **argv) {
    if (argc == 1) {
        return;
    }
    #if 0
    if (strcmp(argv[1], "-C") == 0 || strcmp(argv[1], "--cache-dir") == 0) {
        cache_dir = argv[2];
    }
    #endif

}